'use strict';
CLASS('electron',
    ({
         xxx = 'xxx'
     } = {}) => {

        //监听主进程的消息
        function sss(route, fn) {

        }

        return {
            send: aaa,
            listen: sss
        }
    }
);